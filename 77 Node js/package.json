{
  "name": "import-math-function-test",
  "version": "7.7.7",
  "description": "This file is created because when we want to import a file then we must create a package-json file in order to use import function package-json file os created using npm init function and at last we have to add type : module manually in this file",
  "main": "math.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Sairam",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "random-words": "^2.0.1"
  }
}
